#include <iostream>
#include <string>
#include <sstream>
#include <gtest/gtest.h>
#include "quntoken_api.h"


std::string prepTest(std::string text) {
    std::stringstream inp(text);
    std::string result;
    quntoken_get_str(result, {PREPROC, CONVXML}, &inp);
    return result;
}


std::string hyphenTest(std::string text) {
    std::stringstream inp(text);
    std::string result;
    quntoken_get_str(result, {PREPROC, HYPHEN, CONVXML}, &inp);
    return result;
}


std::string sntTest(std::string text) {
    std::stringstream inp(text);
    std::string result;
    quntoken_get_str(result, {PREPROC, SNT, CONVXML}, &inp);
    return result;
}


std::string sntcorrTest(std::string text) {
    std::stringstream inp(text);
    std::string result;
    quntoken_get_str(result, {PREPROC, SNT, SNTCORR, SNTCORR, CONVXML}, &inp);
    return result;
}

std::string tokenTest(std::string text) {
    std::stringstream inp(text);
    std::string result;
    quntoken_get_str(result, {PREPROC, SNT, SNTCORR, SNTCORR, TOKEN, CONVXML}, &inp);
    return result;
}


${TESTS}


int main(int argc, char **argv) {
    testing::InitGoogleTest(&argc, argv);
    return RUN_ALL_TESTS();
}

// vim:set syntax=cpp:

